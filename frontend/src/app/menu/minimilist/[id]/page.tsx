"use client";

import Minimilist from "@/components/designs/minimilistPreview";
import MenuCompleted from "@/components/menuComponents/menuCompleted";

export default function CompletedMenu() {
  return <MenuCompleted DesignComponent={Minimilist} template="minimilist" />;
}
